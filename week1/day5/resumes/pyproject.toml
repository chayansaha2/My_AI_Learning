[project]
name = "resumes"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "chayansaha2", email = "sahatech311@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
resumes = "resumes:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
